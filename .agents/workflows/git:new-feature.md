---
description: Create a new feature branch from the latest main
---

1. Checkout the main branch, pull the latest changes, and create a new feature branch.
// turbo
```bash
git checkout main && git pull && git checkout -B <new_feature_branch>
```
