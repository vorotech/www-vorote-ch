---
interface Props {
  authorName?: string;
}
const { authorName } = Astro.props;
---

<blockquote class="border-l-4 border-primary pl-4 py-2 my-6 italic text-muted-foreground">
  <slot />
  {authorName && <footer class="text-sm mt-2 not-italic">— {authorName}</footer>}
</blockquote>
